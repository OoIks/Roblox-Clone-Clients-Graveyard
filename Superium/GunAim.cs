// GunAim
using UnityEngine;

public class GunAim : MonoBehaviour
{
	public int borderLeft;

	public int borderRight;

	public int borderTop;

	public int borderBottom;

	private Camera GMKPADLACIL;

	private bool CLIBMAJDOJK;

	public bool OGNKAGDONCH()
	{
		return CLIBMAJDOJK;
	}

	public bool FDKFJCAOIKG()
	{
		return CLIBMAJDOJK;
	}

	public bool LAHOPKOJIPF()
	{
		return CLIBMAJDOJK;
	}

	private void CONOKEINIMP()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 269f)));
		}
	}

	private void HNGCGALADEI()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void ICMGJJHGNDP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool AMACEOBJKGP()
	{
		return CLIBMAJDOJK;
	}

	private void KFFNHFEKOJH()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool HNIPGDDKPLB()
	{
		return CLIBMAJDOJK;
	}

	private void JFEKGAMBHJN()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 932f)));
		}
	}

	public bool MIBDNBDGEKD()
	{
		return CLIBMAJDOJK;
	}

	private void EBMLEALCJEB()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void MHCKEFGNEIP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void FGFLCFGDHNC()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void HEPACNIHKPP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void CBEOODIPCFK()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool FGFELOELBCJ()
	{
		return CLIBMAJDOJK;
	}

	private void LLJHEFBJCCB()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1547f)));
		}
	}

	private void PEIIGOHMMNE()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1466f)));
		}
	}

	private void MEGKBOIOBBI()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1811f)));
		}
	}

	private void IKHFMBMFHHE()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1890f)));
		}
	}

	public bool AGIKKIJNPKB()
	{
		return CLIBMAJDOJK;
	}

	private void HJKKAFDGMCN()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void PLOHMFNJFEE()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 31f)));
		}
	}

	private void KAOFFNAHNMI()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void AEHJDCOHGMK()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void DMOCGMBMLOD()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 508f)));
		}
	}

	private void MJBAPNCKMKM()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void HDIFFAGOLKH()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void FFONODKBHGM()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void ALIEFIMKENH()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1123f)));
		}
	}

	public bool MDDAIFEPKMK()
	{
		return CLIBMAJDOJK;
	}

	private void ICALPAGEIDN()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1966f)));
		}
	}

	public bool NLNHHAAIEDF()
	{
		return CLIBMAJDOJK;
	}

	private void PEDIPAEOKGO()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 499f)));
		}
	}

	public bool KHBGJLADELI()
	{
		return CLIBMAJDOJK;
	}

	private void FGPKKEHAHEE()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void CDJBDJKCMLJ()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void OCINGJALBKB()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1129f)));
		}
	}

	private void KGCMHLHBBAD()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 216f)));
		}
	}

	private void IHEPNDHEABC()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1455f)));
		}
	}

	private void Start()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool PFDOOIOCFIH()
	{
		return CLIBMAJDOJK;
	}

	private void LBMJJECMEDO()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool LOJEEPHEILM()
	{
		return CLIBMAJDOJK;
	}

	private void FHLAEFHPLLA()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void GAJDENHGMCC()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void KHHOLHOOKLA()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 135f)));
		}
	}

	public bool OOGLEIKLAEO()
	{
		return CLIBMAJDOJK;
	}

	public bool PFENJPPGOPL()
	{
		return CLIBMAJDOJK;
	}

	private void LAHBHGPADBH()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 526f)));
		}
	}

	public bool GetIsOutOfBounds()
	{
		return CLIBMAJDOJK;
	}

	private void OPMEGCJJNLC()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void HEPAMDACOJD()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void IFENHOKKPPD()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1242f)));
		}
	}

	private void OAAOPHKMHGN()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void PJMPJPGAEKL()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void AODKAAOMHCM()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool BALJLBNOEIA()
	{
		return CLIBMAJDOJK;
	}

	private void PPBJCIDDNGC()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool HOBFICFIPPO()
	{
		return CLIBMAJDOJK;
	}

	private void LNDNCLLAANE()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool NHIDFEBJFME()
	{
		return CLIBMAJDOJK;
	}

	public bool LDDEJNPCNFM()
	{
		return CLIBMAJDOJK;
	}

	public bool BLEGILPPPLJ()
	{
		return CLIBMAJDOJK;
	}

	public bool NDEBLFFDHGL()
	{
		return CLIBMAJDOJK;
	}

	private void NKHNFBMBCPP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool DPJEEOALKPI()
	{
		return CLIBMAJDOJK;
	}

	private void GFOGLELCKPB()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1081f)));
		}
	}

	private void GAOJDAJACPG()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1736f)));
		}
	}

	public bool NPHIMKICIPL()
	{
		return CLIBMAJDOJK;
	}

	private void GJKLKDPNMDM()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void OIGPCDCPDHL()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1430f)));
		}
	}

	public bool JEPOFLKJJIB()
	{
		return CLIBMAJDOJK;
	}

	private void ENAKOMIABAJ()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1985f)));
		}
	}

	private void BLANFEOIEHH()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1963f)));
		}
	}

	private void GLECMKJEAHB()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 960f)));
		}
	}

	private void HEJJIFBFBAP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void PCJNMBOGMLC()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 118f)));
		}
	}

	private void COBIAJMOANO()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1316f)));
		}
	}

	public bool ABMFKHIOPMF()
	{
		return CLIBMAJDOJK;
	}

	private void EDBBOBMJIMA()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void NAADLFFKHID()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void JCALDGKFAPL()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool NJNNGCLKCPL()
	{
		return CLIBMAJDOJK;
	}

	private void BDMGLCDCEPG()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1410f)));
		}
	}

	private void OFAGDFKNCNL()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1320f)));
		}
	}

	public bool HNINCOCPEKA()
	{
		return CLIBMAJDOJK;
	}

	private void MGLKGPLEAPH()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 779f)));
		}
	}

	public bool KKBKNIHLCDA()
	{
		return CLIBMAJDOJK;
	}

	private void JNFPBGBDKLK()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void ADBCKAPMBJE()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void DBHAOAFNKEB()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void BBNOEKBJJFC()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1775f)));
		}
	}

	private void PCFJNDCKHJK()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 924f)));
		}
	}

	public bool HJPDMBFGDJL()
	{
		return CLIBMAJDOJK;
	}

	private void ONHHKJKAKED()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool ABFFEADPCPM()
	{
		return CLIBMAJDOJK;
	}

	private void MEPOBCHKJHA()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 422f)));
		}
	}

	public bool JHMHJLHCFDH()
	{
		return CLIBMAJDOJK;
	}

	private void Update()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 5f)));
		}
	}

	private void HLKNKOKNOKB()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool LBJPKAGPIDD()
	{
		return CLIBMAJDOJK;
	}

	public bool INCBPBLMIBB()
	{
		return CLIBMAJDOJK;
	}

	private void BCHBGFKOLAL()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 840f)));
		}
	}

	public bool BKEHLPJJOOA()
	{
		return CLIBMAJDOJK;
	}

	private void MHLPDBMIFMO()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = true;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 1878f)));
		}
	}

	private void FFBNKNAHFCG()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void OKHAFADKFCF()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 352f)));
		}
	}

	public bool KJKFGEHODAJ()
	{
		return CLIBMAJDOJK;
	}

	private void PAFEGKADFKI()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void HOABCCJDNGH()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 399f)));
		}
	}

	private void AFJDIBFOBPH()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void CNNDFKKKNLA()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void CKEMKNKNGDH()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	public bool AKGDPGNCEKJ()
	{
		return CLIBMAJDOJK;
	}

	private void BNJNCCDKOPL()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 600f)));
		}
	}

	private void KIMKEHNABCD()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void GKFNBABHHGP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void MFAKPLDNCNP()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void ECLHPLHFMBM()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void JFHIAHILHCI()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 393f)));
		}
	}

	private void NJOGBDOIHFJ()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}

	private void CELLIPPJIAO()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = false;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 635f)));
		}
	}

	private void IHIFIHBDFAB()
	{
		float x = Input.mousePosition.x;
		float y = Input.mousePosition.y;
		if (x <= (float)borderLeft || x >= (float)(Screen.width - borderRight) || y <= (float)borderBottom || y >= (float)(Screen.height - borderTop))
		{
			CLIBMAJDOJK = true;
		}
		else
		{
			CLIBMAJDOJK = false;
		}
		if (!CLIBMAJDOJK)
		{
			base.transform.LookAt(GMKPADLACIL.ScreenToWorldPoint(new Vector3(x, y, 509f)));
		}
	}

	public bool OKNLCBBDBAC()
	{
		return CLIBMAJDOJK;
	}

	private void DDANFAJJNPI()
	{
		GMKPADLACIL = GetComponentInParent<Camera>();
	}
}
