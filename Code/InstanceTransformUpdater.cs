// InstanceTransformUpdater
using UnityEngine;

public class InstanceTransformUpdater : MonoBehaviour
{
	public Part linked;

	private void NIFCICDBJMN()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void DKKLDGBLCBP()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void HDKILADCDDN()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void HFMOPLBNNPO()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void Update()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void CMKLLDPHOEL()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void PEDIPAEOKGO()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void BKDOHCHMGAB()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void GLECMKJEAHB()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void LKEMBNOCHCC()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void MEKCHOAKGLM()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void FBEKMFMKGDM()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void KDKOLGHOAGA()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void BGHIFCGKDPE()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void LMAIIOFFJEB()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void BFADADBGNOP()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void HIKLKEBCMMM()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void GIKBDCGLEEM()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void HOABCCJDNGH()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void BLANFEOIEHH()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void OIGPCDCPDHL()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void MIEJLECJGLG()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void LLJHEFBJCCB()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}

	private void MELMPHNHOJL()
	{
		if (linked != null)
		{
			linked.position = base.transform.position;
			linked.scale = base.transform.localScale;
			linked.rotation = base.transform.eulerAngles;
		}
	}
}
